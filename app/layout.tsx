import type { Metadata, Viewport } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'Meesho - Online Shopping Made Easy',
  description: 'Discover amazing deals on fashion, electronics, home & more',
  keywords: 'shopping, ecommerce, deals, fashion, electronics, home, kitchen',
  authors: [{ name: 'Meesho Clone' }],
}

export const viewport: Viewport = {
  width: 'device-width',
  initialScale: 1,
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        {children}
      </body>
    </html>
  )
}
